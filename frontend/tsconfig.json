{
  "compilerOptions": {
    // --- 1. Project & Module Settings ---
    "target": "ES2020",
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "module": "ESNext",
    "moduleResolution": "bundler", // This is the new, modern standard. Use "node" if this fails.

    // --- 2. React-Specific Settings ---
    "jsx": "react-jsx", // Use "preserve" if your bundler handles JSX transformation (e.g., Babel)

    // --- 3. The CRITICAL Migration Settings ---
    "allowJs": true,     // IMPORTANT: Allows .js and .ts files to co-exist.
    "checkJs": false,    // IMPORTANT: STOPS TypeScript from checking your existing .js files.
    "noEmit": true,      // IMPORTANT: Tells TS to *only* type-check. Your bundler (Vite/Webpack) will create the final JS.
    
    // --- 4. Strictness (The Goal!) ---
    // This is the "best practice" setup. It forces you to write real types 
    // and prevents common bugs (like 'null' errors).
    // It might feel hard at first, but it's the whole point of TS.
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true, // Speeds up compilation by not checking node_modules.

    // --- 5. Quality of Life & Interoperability ---
    "isolatedModules": true, // Required for most bundlers (Vite/Babel)
    "esModuleInterop": true, // Helps with importing older CommonJS modules
    "resolveJsonModule": true, // Lets you import .json files directly
    
    // --- 6. (Optional) Absolute Imports ---
    // This lets you write `import MyButton from 'components/MyButton'`
    // instead of `import MyButton from '../../components/MyButton'`
    "baseUrl": "src",
    // "paths": {
    //   "components/*": ["components/*"],
    //   "utils/*": ["utils/*"]
    // }
  },
  // --- Files to Include/Exclude ---
  "include": [
    "src" // Tell TypeScript to only look inside your 'src' folder
  ],
  "exclude": [
    "node_modules",
    "dist",
    "build"
  ]
}